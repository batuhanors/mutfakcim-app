import { StyleSheet } from "react-native";

export const drawerStyles = StyleSheet.create({
    navLink: {
        flexDirection: "row",
        paddingHorizontal: 10,
        paddingVertical: 16,
        alignItems: "center",
    },
    navText: {
        fontSize: 18,
        paddingHorizontal: 8
    }
})